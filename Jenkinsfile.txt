pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build du projet'
            }
        }

        stage('Run Hello World') {
            steps {
                sh 'hello.sh'
            }
        }
    }
}
